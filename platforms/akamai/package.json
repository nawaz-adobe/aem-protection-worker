{
  "name": "aem-protection-worker-akamai",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build:production": "cp src/main-standalone.js dist/main.js",
    "validate": "node validate-constraints.js",
    "build:validate": "npm run build:production && npm run validate",
    "deploy": "./deploy-production.sh",
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "eslint src/",
    "lint:fix": "eslint src/ --fix"
  },
  "devDependencies": {
    "@eslint/js": "^9.31.0",
    "eslint": "^9.31.0",
    "vitest": "^2.1.5",
    "webpack": "^5.88.0",
    "webpack-cli": "^5.1.0"
  }
}